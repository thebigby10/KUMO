import { LabRepository } from "@/repositories/LabRepository";
import { EnrollmentRepository } from "@/repositories/EnrollmentRepository";
import { InstructorRepository } from "@/repositories/InstructorRepository";
import { AnnouncementRepository } from "@/repositories/AnnouncementRepository";
import { LabWorkRepository } from "@/repositories/LabWorkRepository";
import { generateLabCode } from "@/lib/utils";
import { db } from "@/models/models";

export class EnrollmentController {
  // static async addEnrollment(email, labId) {}
  // findInstructor(labId, email);
  // getMembers(labId);
  // removeStudent(labId, email);
  // leaveLab(labId, email);
  // addInstructor(labId, email);
}
